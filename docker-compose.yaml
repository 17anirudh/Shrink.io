services:
  spring:
    build: ./spring-boot
    container_name: java-backend
    ports:
      - "8100:8100"
    depends_on:
      - mariadb

  flask:
    build: ./flask
    container_name: python-backend
    ports:
      - "5400:5400"
    depends_on:
      - mariadb

  mariadb:
    image: bitnami/mariadb:10.11.11-debian-12-r11
    container_name: mariadb-db
    environment:
      - MARIADB_ROOT_PASSWORD=root@2004
      - MARIADB_DATABASE=uri
    volumes:
      - db_data:/bitnami/mariadb
    ports:
      - "3306:3306"

volumes:
  db_data:
